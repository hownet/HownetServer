using System;
using System.Data;
using LTP.Common;
using Hownet.Model;
using System.Collections;
using System.Text;
namespace Hownet.BLL
{
   public class CheckEcho
    {
       //public void CheckEcho(ArrayList FiledList, DataTable dt, string TableName)
       //{

       //    if (FiledList.Count > 0 && dt.Rows.Count > 0)
       //    {
       //        DataRow[] drs = dt.Select("A <> 1");
       //        DataTable tem = dt.Clone();
       //        foreach (DataRow dr in drs)
       //        {
       //            tem.Rows.Add(dr);
       //        }
       //        for(int i=0;i<tem.Rows.Count;i++)
       //            if (tem.Rows[i]["A"].ToString() == string.Empty)
       //            {
       //                StringBuilder strSql = new StringBuilder();
       //                strSql.Append("select count(ID) as qwe from ");
       //                strSql.Append(TableName);
       //                strSql.Append(" Where ( ");
       //                strSql.Append(FiledList[0].ToString());
       //                strSql.Append(
       //                for (int j = 1; j < FiledList.Count; j++)
       //                {
       //                    strSql.Append(",");
       //                    strSql.Append(FiledList[j].ToString());
       //                }
       //            }
       //    }
       //}
    }
}
